import { BoardRunner } from "@google-labs/breadboard";
export type Options = {
    output: string;
    watch?: boolean;
    save?: boolean;
};
export declare abstract class Loader {
    makeFromSource(filename: string, source: string, options?: Options): Promise<{
        boardJson: string;
        board: BoardRunner;
    }>;
    makeFromFile(filePath: string): Promise<{
        boardJson: string;
        board: BoardRunner;
    }>;
    loadBoardFromModule(file: string): Promise<BoardRunner>;
    loadBoardFromSource(filename: string, source: string, options?: Options): Promise<BoardRunner>;
    abstract load(filePath: string, options: Options): Promise<BoardRunner>;
}
//# sourceMappingURL=loader.d.ts.map